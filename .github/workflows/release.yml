name: Release

on:
  push:
    tags: ['v*']

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact: kannaka-crystal-windows-x86_64.exe
          # MUSL static binaries — no glibc dependency at all, so the same
          # binary runs on every Linux regardless of glibc version (the
          # lesson learned in kannaka-memory: gnu builds from ubuntu-22.04
          # fail on Oracle Linux 9's older glibc).
          - target: x86_64-unknown-linux-musl
            os: ubuntu-22.04
            artifact: kannaka-crystal-linux-x86_64
          - target: aarch64-unknown-linux-musl
            os: ubuntu-22.04
            artifact: kannaka-crystal-linux-aarch64
          - target: x86_64-apple-darwin
            os: macos-latest
            artifact: kannaka-crystal-macos-x86_64
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: kannaka-crystal-macos-aarch64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Set up musl cross toolchain (linux)
        if: contains(matrix.target, 'linux-musl')
        uses: taiki-e/setup-cross-toolchain-action@v1
        with:
          target: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }} --bin kannaka-crystal

      - name: Rename artifact + emit SHA-256 sidecar
        shell: bash
        # Sidecar format matches `sha256sum`: "<hex-digest>  <filename>" —
        # same convention as kannaka-memory releases, so installers and
        # update flows can verify downloads before replacing binaries.
        run: |
          ext=""
          if [[ "${{ matrix.target }}" == *windows* ]]; then ext=".exe"; fi
          cp "target/${{ matrix.target }}/release/kannaka-crystal${ext}" "${{ matrix.artifact }}"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${{ matrix.artifact }}" > "${{ matrix.artifact }}.sha256"
          else
            shasum -a 256 "${{ matrix.artifact }}" > "${{ matrix.artifact }}.sha256"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

      - name: Upload SHA-256 sidecar
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}.sha256
          path: ${{ matrix.artifact }}.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            kannaka-crystal-windows-x86_64.exe/kannaka-crystal-windows-x86_64.exe
            kannaka-crystal-windows-x86_64.exe.sha256/kannaka-crystal-windows-x86_64.exe.sha256
            kannaka-crystal-linux-x86_64/kannaka-crystal-linux-x86_64
            kannaka-crystal-linux-x86_64.sha256/kannaka-crystal-linux-x86_64.sha256
            kannaka-crystal-linux-aarch64/kannaka-crystal-linux-aarch64
            kannaka-crystal-linux-aarch64.sha256/kannaka-crystal-linux-aarch64.sha256
            kannaka-crystal-macos-x86_64/kannaka-crystal-macos-x86_64
            kannaka-crystal-macos-x86_64.sha256/kannaka-crystal-macos-x86_64.sha256
            kannaka-crystal-macos-aarch64/kannaka-crystal-macos-aarch64
            kannaka-crystal-macos-aarch64.sha256/kannaka-crystal-macos-aarch64.sha256
          generate_release_notes: true
